# قائمة المهام | Tasks
## المصدر: plan.md + data-model.md + spec.md (001-ai-ad-platform)

**قاعدة الترتيب:** كل مهمة اختبار تسبق مهمة التنفيذ المرتبطة بها (TDD). المهام المعلَّمة بـ `[P]` قابلة للتنفيذ بالتوازي (ملفات/وحدات مختلفة، لا تبعية مباشرة بينها) — يمكن توزيعها على أدوات ذكاء اصطناعي مختلفة تنفذ كل واحدة بمعزل عن الأخرى.

---

## المرحلة 3.1 — الإعداد (Setup)

- [ ] **T001** إنشاء بنية المستودع (monorepo) طبقًا لهيكل `plan.md`: `apps/web`, `apps/api`, `workers/generation-worker`, `packages/ai-gateway`, `packages/credits`, `packages/shared-types`.
- [ ] **T002** تهيئة قاعدة PostgreSQL ومهاجرات (migrations) فارغة أولية.
- [ ] **T003** تهيئة Redis + طابور المهام (BullMQ أو مكافئ) للاتصال بين `apps/api` و`workers/generation-worker`.
- [ ] **T004 [P]** إعداد أدوات فحص الجودة (Linter + Formatter) لكل من `apps/web` و`apps/api`.
- [ ] **T005 [P]** إعداد بيئة `docker-compose.yml` تشغّل قاعدة البيانات + Redis + التخزين المحلي للتطوير.

---

## المرحلة 3.2 — عقود الواجهة أولاً ⚠️ (Contracts First — يجب أن تفشل قبل التنفيذ)

> قاعدة صارمة: كل مهمة هنا تكتب اختبارًا لعقد واجهة (Interface Contract) يجب أن **يفشل** قبل وجود أي تنفيذ فعلي. لا تُكتب أي منطق تنفيذي في هذه المرحلة.

- [ ] **T006 [P]** كتابة اختبار عقد لخدمة `VideoGenerationProvider` داخل `packages/ai-gateway` (مدخل: صورة + سيناريو + لهجة، مخرج: job_id).
- [ ] **T007 [P]** كتابة اختبار عقد لخدمة `ImageGenerationProvider` (إعلان صوري) داخل `packages/ai-gateway`.
- [ ] **T008 [P]** كتابة اختبار عقد لخدمة `ArabicTTSProvider` (مدخل: نص + لهجة، مخرج: ملف صوتي).
- [ ] **T009 [P]** كتابة اختبار عقد لخدمة `BackgroundRemovalProvider`.
- [ ] **T010 [P]** كتابة اختبار عقد لخدمة `ImageUpscaleProvider`.
- [ ] **T011 [P]** كتابة اختبار عقد لخدمة `ScriptAnalysisProvider` (تحليل صورة → سيناريو مقترح).
- [ ] **T012 [P]** كتابة اختبار عقد لخدمة `CreditWalletService` (خصم/إضافة/استرجاع، منع الرصيد السالب).

---

## المرحلة 3.3 — اختبارات التكامل (Integration Tests) ⚠️ يجب أن تفشل قبل التنفيذ

مبنية مباشرة على "سيناريوهات القبول" في `spec.md`.

- [ ] **T013 [P]** اختبار تكامل: تسجيل مستخدم جديد يحصل على رصيد تجريبي (Acceptance Scenario 1).
- [ ] **T014 [P]** اختبار تكامل: رفع صورة → عرض سيناريو مقترح دون خصم كريدت (Scenario 2).
- [ ] **T015 [P]** اختبار تكامل: تأكيد سيناريو → إنشاء `GenerationJob` بحالة `queued` فورًا دون حجب الطلب (Scenario 3).
- [ ] **T016 [P]** اختبار تكامل: اكتمال مهمة فيديو → تحديث حالة `Ad` إلى `done` وربط `output_url` (Scenario 4).
- [ ] **T017 [P]** اختبار تكامل: تجاوز سقف الأداة المجانية → رفض الطلب برسالة ترقية دون حجب باقي الميزات (Scenario 5).
- [ ] **T018 [P]** اختبار تكامل: استرجاع الكريدت تلقائيًا عند فشل `GenerationJob` (FR-014).

---

## المرحلة 3.4 — التنفيذ الأساسي (Core Implementation) — بعد فشل كل الاختبارات أعلاه فقط

### نموذج البيانات
- [ ] **T019 [P]** نموذج `User` + مهاجرة قاعدة بيانات.
- [ ] **T020 [P]** نموذج `CreditWallet` + `CreditTransaction` + مهاجرة.
- [ ] **T021 [P]** نموذج `BrandKit` + مهاجرة.
- [ ] **T022 [P]** نموذج `Ad` + انتقالات الحالة المسموحة فقط (`draft_script → queued → processing → done|failed`).
- [ ] **T023 [P]** نموذج `GenerationJob` + مهاجرة.
- [ ] **T024 [P]** نموذج `Plan` و`Template` + بيانات أولية (seed) للباقات الأربع.

### خدمة الكريدت (يحقق `T012`)
- [ ] **T025** تنفيذ `CreditWalletService`: خصم قبل أي استدعاء AI، منع الرصيد السالب، تسجيل كل حركة.
- [ ] **T026** تنفيذ منطق الاسترجاع التلقائي عند فشل `GenerationJob` (يحقق `T018`).

### طبقة الذكاء الاصطناعي (يحقق T006–T011)
- [ ] **T027** تنفيذ `VideoGenerationProvider` (Adapter أول — المزوّد الأساسي المُختار في `research.md`).
- [ ] **T028 [P]** تنفيذ `ImageGenerationProvider`.
- [ ] **T029 [P]** تنفيذ `ArabicTTSProvider` (يدعم قيم `preferred_dialect` كاملة).
- [ ] **T030 [P]** تنفيذ `BackgroundRemovalProvider` (نموذج مستضاف ذاتيًا حسب قرار `research.md`).
- [ ] **T031 [P]** تنفيذ `ImageUpscaleProvider` (نموذج مستضاف ذاتيًا).
- [ ] **T032 [P]** تنفيذ `ScriptAnalysisProvider`.

### نقاط النهاية (API Endpoints) — تحقق اختبارات التكامل T013–T017
- [ ] **T033** `POST /auth/register` + منح رصيد تجريبي تلقائي.
- [ ] **T034** `POST /ads/analyze-image` → يعيد سيناريو مقترح دون خصم كريدت.
- [ ] **T035** `POST /ads/confirm` → يتحقق من الرصيد عبر `CreditWalletService`، يخصم، وينشئ `Ad` + `GenerationJob` بحالة `queued`.
- [ ] **T036** `GET /ads/:id/status` → حالة المعالجة اللحظية.
- [ ] **T037** `GET /ads` → أرشيف إعلانات المستخدم.
- [ ] **T038** `POST /tools/background-removal` و`POST /tools/upscale` مع فحص سقف الاستخدام المجاني الشهري قبل التنفيذ.

### العامل الخلفي (Worker)
- [ ] **T039** تنفيذ `generation-worker`: يسحب المهام من الطابور، يستدعي `ai-gateway` المناسب، يحدّث حالة `Ad`/`GenerationJob`، ويستدعي استرجاع الكريدت عند الفشل.

---

## المرحلة 3.5 — الواجهة (Frontend)

- [ ] **T040** صفحة رفع صورة + عرض السيناريو المقترح (RTL بالكامل).
- [ ] **T041** شاشة اختيار اللهجة + تأكيد استهلاك الكريدت قبل التوليد.
- [ ] **T042** شاشة تتبع حالة المعالجة (بدون حجب باقي التطبيق) + إشعار عند الاكتمال.
- [ ] **T043** أرشيف "إعلاناتي" مع خيار تنزيل/مشاركة بمقاسات المنصات المختلفة.
- [ ] **T044** واجهة إدارة "هوية العلامة" (Brand Kit) وربطها التلقائي بإعلانات جديدة.
- [ ] **T045** واجهة الباقات وشراء الكريدت.

---

## المرحلة 3.6 — التلميع والتحقق النهائي (Polish)

- [ ] **T046 [P]** اختبارات وحدة إضافية لحالات حرجة (صورة منخفضة الجودة، تزامن خصم الرصيد).
- [ ] **T047** تنفيذ سيناريو `quickstart.md` كاملًا يدويًا والتأكد من نجاح كل خطوة.
- [ ] **T048 [P]** مراجعة أداء: زمن استجابة `analyze-image` وزمن بدء معالجة `GenerationJob`.
- [ ] **T049** مراجعة أمنية: التأكد أن بيانات الدفع لا تُخزَّن مباشرة (متوافق مع القيد التقني بالدستور).
- [ ] **T050** إزالة أي تكرار كود بين Adapters المختلفة في `ai-gateway`.

---

## مصفوفة التبعية المختصرة (Dependencies)

```
T001-T005 (إعداد) → T006-T012 (عقود) → T013-T018 (تكامل)
   → T019-T024 (نماذج بيانات، [P] فيما بينها)
   → T025-T026 (كريدت) يعتمد على T020
   → T027-T032 (AI Adapters، [P] فيما بينها) يعتمد على T006-T011
   → T033-T038 (API) يعتمد على T019-T032
   → T039 (Worker) يعتمد على T027-T032 و T025-T026
   → T040-T045 (واجهة) يعتمد على T033-T038
   → T046-T050 (تلميع) يعتمد على اكتمال كل ما سبق
```

## ملاحظة نقل المهام لأدوات ذكاء اصطناعي أخرى

كل مهمة معلَّمة بـ `[P]` مستقلة بملفها/وحدتها ومرتبطة بعقد واجهة واضح — يمكن نسخ نص المهمة مع الملف المرجعي المرتبط (`data-model.md` أو قسم العقد في `plan.md`) وتنفيذها عبر أداة ذكاء اصطناعي مختلفة دون الحاجة لتحميلها بسياق المشروع الكامل، طالما تلتزم بالعقد المحدد في المرحلة 3.2.
