# مواصفة الميزة: منصة إنشاء إعلانات الفيديو والصور بالذكاء الاصطناعي
## Feature Specification

**رمز الميزة (Feature Branch):** `001-ai-ad-platform`
**تاريخ الإنشاء:** 2026-09-24
**الحالة (Status):** Draft — بانتظار المراجعة
**المدخل (Input):** طلب مستخدم لبناء منصة SaaS تحوّل صورة منتج إلى إعلان فيديو/صورة جاهز باستخدام الذكاء الاصطناعي، موجهة للسوق العربي، بديل تنافسي لمنصات مثل "عقل".

> ⚠️ قاعدة SpecKit صارمة: هذا الملف يصف **ماذا** يحتاج المستخدم و**لماذا**، ولا يذكر أي تقنية أو مكتبة أو API تحديدًا. التفاصيل التقنية موجودة حصرًا في `plan.md` و`research.md`.

---

## سيناريوهات المستخدم والاختبار (User Scenarios & Testing)

### القصة الأساسية (Primary User Story)
صاحب مشروع صغير (مطعم، متجر، عيادة) يملك صورة واحدة لمنتجه ولا يملك ميزانية تصوير أو فريق مونتاج. يرفع الصورة على المنصة، يختار نوع الإعلان (فيديو أو صورة ثابتة)، تحلل المنصة الصورة وتقترح سيناريو إعلاني، يوافق أو يعدّل بكلمة واحدة، ثم يحصل خلال دقائق على إعلان جاهز بصوت عربي احترافي وجاهز للنشر مباشرة على السوشيال ميديا.

### سيناريوهات القبول (Acceptance Scenarios)

1. **بالنظر إلى** مستخدم جديد غير مسجّل، **عندما** يفتح المنصة لأول مرة، **فإن** يُطلب منه إنشاء حساب مجاني، ويحصل تلقائيًا على رصيد تجريبي (إعلانات صورية + عدد محدود من عمليات إزالة الخلفية/تحسين الجودة).
2. **بالنظر إلى** مستخدم رفع صورة منتج، **عندما** يطلب "إنشاء إعلان فيديو"، **فإن** النظام يحلّل الصورة، يعرض سيناريو مقترح (نص + وصف مشاهد) خلال ثوانٍ، وينتظر موافقة المستخدم قبل بدء التوليد الفعلي.
3. **بالنظر إلى** سيناريو تمت الموافقة عليه، **عندما** يبدأ توليد الفيديو، **فإن** المستخدم يرى حالة تقدّم واضحة (قيد المعالجة) ولا تُحجَب الواجهة، ويصله تنبيه عند الاكتمال.
4. **بالنظر إلى** فيديو مكتمل، **عندما** يفتحه المستخدم، **فإن** يجد صوتًا عربيًا مطابقًا للهجة التي اختارها، ومشاهد سينمائية مرتبطة بمنتجه، وزر تنزيل/مشاركة مباشر بالمقاس المناسب لكل منصة (تيك توك، انستغرام، فيسبوك).
5. **بالنظر إلى** مستخدم في الباقة المجانية استنفد رصيد الأدوات المجانية (إزالة خلفية/تحسين جودة)، **عندما** يحاول استخدامها مجددًا، **فإن** يظهر له بوضوح أنه استنفد الحد المجاني وخيار ترقية الباقة، دون حجب بقية ميزات المنصة.
6. **بالنظر إلى** مستخدم أنشأ عدة إعلانات، **عندما** يفتح "إعلاناتي"، **فإن** يجد أرشيفًا لكل الإعلانات السابقة قابلة لإعادة التنزيل أو إعادة التوليد بتعديل بسيط.

### حالات حرجة (Edge Cases)
- ماذا يحدث عندما تكون الصورة المرفوعة منخفضة الجودة جدًا أو غير واضحة لتحليل الذكاء الاصطناعي؟
- ماذا يحدث عندما ينفد رصيد الكريدت في منتصف عملية توليد فيديو طويلة؟
- ماذا يحدث عند فشل مزوّد الذكاء الاصطناعي (خطأ خارجي) أثناء المعالجة — هل يُرجَّع الكريدت المخصوم تلقائيًا؟
- كيف يتعامل النظام مع محتوى مرفوع مخالف (صور غير لائقة أو منتجات محظورة إعلانيًا)؟
- ماذا يحدث عند طلب مستخدمَين لنفس الحساب توليد إعلانين بنفس اللحظة (تزامن الرصيد)؟

---

## المتطلبات (Requirements)

### متطلبات وظيفية (Functional Requirements)

- **FR-001**: يجب أن يسمح النظام للمستخدم برفع صورة منتج واحدة على الأقل كمدخل لبدء أي إعلان.
- **FR-002**: يجب أن يحلّل النظام الصورة المرفوعة تلقائيًا وأن يقترح سيناريو إعلاني نصي (Hook + جسم الرسالة + دعوة لاتخاذ إجراء).
- **FR-003**: يجب أن يعرض النظام السيناريو المقترح للمستخدم للموافقة أو التعديل قبل استهلاك أي رصيد لتوليد الفيديو الفعلي.
- **FR-004**: يجب أن يولّد النظام إعلان فيديو قصيرًا (مدة قابلة للتحديد ضمن حدود الباقة) يتضمن مشاهد مرتبطة بالمنتج وتعليقًا صوتيًا عربيًا.
- **FR-005**: يجب أن يتيح النظام للمستخدم اختيار اللهجة العربية للصوت من قائمة محددة (فصحى، خليجي، عراقي، مصري، شامي كحد أدنى).
- **FR-006**: يجب أن يولّد النظام إعلانات صورية ثابتة (بوست/ستوري) بمقاسات جاهزة لمنصات التواصل الشائعة.
- **FR-007**: يجب أن يوفر النظام أداة "إزالة الخلفية" كميزة مجانية بعدد استخدامات محدود شهريًا لكل مستخدم.
- **FR-008**: يجب أن يوفر النظام أداة "تحسين جودة الصورة" (رفع الدقة) كميزة مجانية بعدد استخدامات محدود شهريًا لكل مستخدم.
- **FR-009**: يجب أن يمتلك كل مستخدم رصيد كريدت (Credits) يُخصم عند كل عملية توليد فعلية، مع عرض واضح للرصيد المتبقي قبل كل عملية.
- **FR-010**: يجب أن يعرض النظام للمستخدم التكلفة (بالكريدت) قبل تأكيد أي عملية توليد، وأن يطلب تأكيدًا صريحًا.
- **FR-011**: يجب أن يوفر النظام باقات اشتراك/شراء كريدت متعددة المستويات (مجانية، أساسية، احترافية، وكالات).
- **FR-012**: يجب أن يحتفظ النظام بأرشيف لكل الإعلانات (فيديو وصورة) التي أنشأها المستخدم، قابلة للتنزيل لاحقًا.
- **FR-013**: يجب أن يُشعِر النظام المستخدم فور اكتمال عملية توليد غير متزامنة (فيديو).
- **FR-014**: يجب أن يعيد النظام الكريدت المخصوم تلقائيًا إلى رصيد المستخدم في حال فشلت عملية التوليد لسبب تقني خارج عن سيطرة المستخدم. [NEEDS CLARIFICATION: هل الاسترجاع فوري تلقائي بالكامل أم يمر بمراجعة عند تجاوز مبلغ معين؟]
- **FR-015**: يجب أن يمنع النظام معالجة صور تحتوي محتوى محظور وفق سياسة استخدام مُعرَّفة مسبقًا. [NEEDS CLARIFICATION: من يحدد قائمة المحتوى المحظور، وهل الفحص آلي بالكامل أم بمراجعة بشرية عشوائية؟]
- **FR-016**: يجب أن يتيح النظام للمستخدمين على باقة "وكالات" إدارة إعلانات متعددة العملاء ضمن حساب واحد (تعدد ملفات علامة تجارية / Brand Kits).
- **FR-017**: يجب أن يدعم النظام حفظ "هوية علامة تجارية" (ألوان، شعار، صوت مفضل) تُطبَّق تلقائيًا على الإعلانات الجديدة لنفس المستخدم.

### الكيانات الأساسية (Key Entities)

- **المستخدم (User)**: يمثل صاحب الحساب؛ يحمل رصيد الكريدت، الباقة الحالية، اللهجة المفضلة، سجل الإعلانات.
- **الإعلان (Ad)**: نتيجة توليد واحدة (فيديو أو صورة)؛ مرتبط بمستخدم، بصورة مصدر، بسيناريو نصي، وبحالة معالجة.
- **هوية العلامة (Brand Kit)**: مجموعة إعدادات محفوظة (شعار، ألوان، صوت مفضل) تُطبَّق على إعلانات مستخدم أو عميل معيّن.
- **محفظة الكريدت (Credit Wallet)**: رصيد المستخدم من وحدات الاستخدام، وسجل الحركات (خصم/إضافة/استرجاع).
- **الباقة (Package/Plan)**: مستوى اشتراك يحدد حدود الاستخدام الشهرية وسعر الكريدت.
- **القالب (Template)**: بنية سيناريو جاهزة مرتبطة بقطاع نشاط معيّن (مطعم، عيادة، متجر...).

---

## قائمة مراجعة القبول (Review & Acceptance Checklist)

- [x] لا يحتوي هذا الملف أي إشارة لتقنية أو API أو مكتبة برمجية محددة
- [x] كل متطلب وظيفي قابل للاختبار (testable) بشكل مستقل
- [ ] جميع علامات `[NEEDS CLARIFICATION]` تم حلّها قبل الموافقة على الانتقال إلى `plan.md`
- [x] السيناريوهات تغطي المسار الأساسي وحالات الفشل الحرجة
- [ ] تمت مراجعة هذا الملف والموافقة عليه من صاحب المنتج قبل بدء التخطيط التقني
