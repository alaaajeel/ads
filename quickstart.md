# سيناريو التحقق (Quickstart Validation)

هذا الملف يمثّل الاختبار اليدوي الذي يُثبت أن المسار الأساسي من `spec.md` يعمل فعليًا بعد التنفيذ. يُستخدم كقائمة قبول نهائية قبل اعتبار MVP جاهزًا.

## المتطلبات المسبقة
- حساب مستخدم جديد بحالة "مجانية" ورصيد كريدت تجريبي.
- صورة منتج حقيقية (jpg/png) بجودة معقولة.

## خطوات التحقق

1. **تسجيل الدخول** بحساب جديد → يجب أن يظهر رصيد كريدت تجريبي واضح في الواجهة الرئيسية.
2. **رفع صورة منتج** والضغط على "إنشاء إعلان جديد" → يجب أن يظهر مؤشر تحليل صورة (لا حجب كامل للواجهة).
3. **مراجعة السيناريو المقترح** الذي يعرضه النظام → يجب أن يحتوي Hook + جسم رسالة + دعوة لإجراء، وأن يكون قابلًا للتعديل النصي المباشر.
4. **اختيار اللهجة العربية** من قائمة واضحة قبل تأكيد التوليد.
5. **تأكيد التوليد** → يجب أن يظهر للمستخدم عدد الكريدت الذي سيُخصم *قبل* الضغط على "تأكيد"، وأن يُطلب تأكيد صريح.
6. **بعد التأكيد** → يجب أن يظهر رصيد الكريدت وقد انخفض فورًا، وأن تظهر حالة "قيد المعالجة" دون تجميد الواجهة (يمكن للمستخدم مغادرة الصفحة والعودة لاحقًا).
7. **عند الاكتمال** → يجب أن يصل إشعار، وأن يظهر الفيديو/الصورة في "إعلاناتي" بحالة "جاهز"، بصوت عربي مطابق للهجة المختارة.
8. **تجربة الأدوات المجانية**: استخدام "إزالة الخلفية" و"تحسين الجودة" حتى الوصول للسقف الشهري → يجب أن تظهر رسالة واضحة بانتهاء الحد المجاني مع خيار ترقية، دون منع باقي الميزات.
9. **اختبار حالة الفشل**: محاكاة فشل مزوّد الذكاء الاصطناعي (بيئة اختبار) → يجب أن يُسترجَع الكريدت المخصوم تلقائيًا إلى رصيد المستخدم (FR-014).

## معيار النجاح
تمر جميع الخطوات أعلاه بدون تدخل يدوي من فريق التطوير، وبدون أي نقطة تُحجَب فيها الواجهة الرئيسية بالكامل أثناء انتظار توليد الفيديو.
