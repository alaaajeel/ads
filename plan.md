# خطة التنفيذ: منصة إنشاء إعلانات الفيديو والصور بالذكاء الاصطناعي
## Implementation Plan

**المدخل (Input):** `specs/001-ai-ad-platform/spec.md`
**المخرجات (Outputs):** `research.md`, `data-model.md`, `tasks.md`, `quickstart.md`

---

## السياق التقني (Technical Context)

| البند | القيمة |
|---|---|
| اللغة/الإطار (الواجهة) | Next.js (React) + TypeScript، دعم RTL كامل افتراضيًا |
| اللغة/الإطار (الخلفية) | Node.js (NestJS) أو Python (FastAPI) — القرار في research.md |
| قاعدة البيانات | PostgreSQL |
| طابور المهام (Queue) | Redis + BullMQ (أو ما يعادلها) لعمليات التوليد غير المتزامنة |
| تخزين الوسائط | تخزين كائنات متوافق مع S3 (Cloudflare R2 مقترح لتقليل التكلفة) |
| طبقة الذكاء الاصطناعي | Adapter Layer داخلي يغلّف: توليد فيديو، تحليل صورة/سيناريو، TTS عربي، إزالة خلفية، تحسين جودة |
| المصادقة | JWT + OAuth اختياري (Google) |
| الدفع | بوابة دفع دولية (Stripe/Paddle) + بحث بوابة محلية عراقية/خليجية [NEEDS CLARIFICATION: السوق المستهدف الأساسي لتحديد بوابة الدفع المحلية] |
| النشر | حاوية Docker + استضافة سحابية (VPS أو منصة PaaS) |
| نوع المشروع | تطبيق ويب (Web App) — واجهة + خلفية + عامل خلفي (Worker) للمهام غير المتزامنة |

---

## فحص التوافق مع الدستور (Constitution Check)

يجب المرور بهذا الفحص قبل الموافقة على الخطة وقبل الانتقال لمرحلة `tasks.md`.

| المبدأ | الفحص | الحالة |
|---|---|---|
| I. البساطة أولاً | هل مسار "من الصورة إلى الإعلان" ≤ 3 خطوات رئيسية بالواجهة؟ | ✅ نعم (رفع → موافقة على السيناريو → توليد) |
| II. طبقة AI مستقلة عن المزوّد | هل كل استدعاء AI يمر عبر Adapter داخلي وليس مباشرة؟ | ✅ مخطَّط له في البنية أدناه |
| III. معالجة غير متزامنة | هل كل توليد فيديو/صورة يمر عبر Queue؟ | ✅ إلزامي لكل مهمة توليد |
| IV. تحكم بالتكلفة | هل يوجد خصم كريدت + سقف استخدام قبل كل استدعاء مدفوع؟ | ✅ مخطَّط ضمن Credit Wallet Service |
| V. وعي باللهجات | هل اختيار اللهجة ظاهر في مسار التوليد الأساسي وليس إعداد مخفي؟ | ✅ خطوة صريحة في FR-005 |
| VI. قابلية اختبار مستقل | هل كل خدمة (Video Gen, Image Gen, Background Removal, Credits) قابلة للاختبار بمعزل؟ | ✅ عبر عقود واجهة (interface contracts) منفصلة لكل خدمة |

**نتيجة الفحص:** لا يوجد انتهاك يستدعي تسجيلًا في "Complexity Tracking" — الخطة متوافقة بالكامل مع الدستور.

---

## بنية المشروع (Project Structure)

```
ai-ad-platform/
├── memory/
│   └── constitution.md
├── specs/
│   └── 001-ai-ad-platform/
│       ├── spec.md
│       ├── plan.md
│       ├── research.md
│       ├── data-model.md
│       ├── quickstart.md
│       └── tasks.md
├── apps/
│   ├── web/                 # واجهة Next.js
│   └── api/                 # خلفية FastAPI/NestJS
├── workers/
│   └── generation-worker/   # عامل معالجة المهام غير المتزامنة (توليد فيديو/صورة/صوت)
├── packages/
│   ├── ai-gateway/          # طبقة التجريد لمزوّدي الذكاء الاصطناعي (المبدأ II)
│   ├── credits/             # خدمة محفظة الكريدت (المبدأ IV)
│   └── shared-types/        # أنواع بيانات مشتركة بين الواجهة والخلفية
└── infra/
    └── docker-compose.yml
```

**سبب هذه البنية:** فصل خدمة الذكاء الاصطناعي (`ai-gateway`) وخدمة الكريدت (`credits`) كحزم مستقلة يحقق مباشرة المبدأين II وIV وVI من الدستور — يمكن اختبار كل منهما بمعزل، ويمكن استبدال أي مزوّد AI دون لمس منطق المنتج.

---

## المرحلة 0: البحث (تفاصيل كاملة في `research.md`)

قرارات تحتاج حسمًا قبل البدء بالتنفيذ الفعلي:
1. مزوّد توليد الفيديو الأساسي ومزوّد بديل احتياطي.
2. مزوّد TTS العربي الأفضل جودة للهجات المستهدفة.
3. استراتيجية استضافة أدوات إزالة الخلفية/تحسين الجودة (مفتوحة المصدر ذاتية الاستضافة مقابل API مدفوع).
4. بوابة الدفع المحلية المناسبة للسوق المستهدف.

## المرحلة 1: التصميم (تفاصيل كاملة في `data-model.md` و`quickstart.md`)

- نموذج البيانات الكامل للكيانات المذكورة في `spec.md`.
- عقود واجهة (Interface Contracts) لكل خدمة داخل `ai-gateway` (توليد فيديو، توليد صورة، TTS، إزالة خلفية، تحسين جودة) — كل عقد مستقل يسمح بتنفيذه عبر أي أداة ذكاء اصطناعي أخرى بمعزل عن بقية النظام، تحقيقًا لمتطلب "قابلية النقل بين أدوات الذكاء الاصطناعي".
- سيناريو تحقق يدوي (`quickstart.md`) يغطي المسار الأساسي الكامل من `spec.md`.

## المرحلة 2: نهج تخطيط المهام (Task Planning Approach)

يُبنى `tasks.md` لاحقًا وفق الترتيب التالي (لا يُنفَّذ هنا، فقط يُوصَف النهج):
- كل كيان في `data-model.md` → مهمة نموذج بيانات + مهمة اختبار مستقلة [P].
- كل عقد واجهة في `ai-gateway` → مهمة تنفيذ Adapter + مهمة اختبار وهمي (mock) مستقلة [P].
- كل سيناريو قبول في `spec.md` → مهمة اختبار تكامل (integration test) قبل مهمة التنفيذ المرتبطة به (ترتيب TDD).
- المهام القابلة للتوازي تُعلَّم بـ `[P]` (ملفات مختلفة، لا تبعية بينها).

---

## تتبع التعقيد (Complexity Tracking)

*(فارغ حاليًا — لا يوجد انتهاك للدستور يستدعي تبريرًا)*

| الانتهاك | لماذا كان ضروريًا | البديل الأبسط ولماذا رُفض |
|---|---|---|
| — | — | — |
