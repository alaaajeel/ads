# دستور المشروع | Project Constitution
## منصة إعلانات الفيديو والصور بالذكاء الاصطناعي (AI Ad Platform)

**النسخة (Version):** 1.0.0
**التاريخ (Ratified):** 2026-09-24
**آخر تعديل (Last Amended):** 2026-09-24

هذا المستند يمثل القواعد الحاكمة العليا للمشروع. أي قرار تصميم أو تنفيذ (Spec أو Plan أو Task) يجب أن يتوافق مع هذه المبادئ، وأي تعارض يُحل لصالح الدستور، أو يُعدَّل الدستور رسميًا عبر عملية التعديل الموضحة أدناه.

---

## المبادئ الأساسية (Core Principles)

### I. البساطة أولاً (Simplicity-First) — NON-NEGOTIABLE
كل ميزة تُقاس بعدد الخطوات اللازمة لإنجازها من قبل مستخدم غير تقني. الهدف: "من الصورة إلى الإعلان الجاهز" في أقل من 3 ضغطات. أي تصميم يضيف خطوة وسيطة يجب أن يبرر نفسه بوضوح أو يُرفض. الواجهة يجب أن تُبنى بلغة عربية (RTL) بالكامل كافتراضي أساسي، وليس كترجمة لاحقة.

### II. طبقة ذكاء اصطناعي مستقلة عن المزوّد (Provider-Agnostic AI Layer)
لا يُسمح لأي منطق عمل (Business Logic) بالاستدعاء المباشر لـ API خاص بمزوّد ذكاء اصطناعي معيّن (Higgsfield, ElevenLabs, إلخ). كل استدعاء يمر عبر طبقة تجريد داخلية (Adapter/Gateway) بحيث يمكن استبدال أو إضافة مزوّد جديد دون تعديل أي كود في منطق المنتج. هذا شرط أساسي لأن سوق نماذج الذكاء الاصطناعي يتغير بسرعة وأي اعتماد صلب (hard dependency) على مزوّد واحد يُعتبر خطأ معماري.

### III. المعالجة غير المتزامنة افتراضيًا (Async-by-Default)
أي عملية توليد فيديو أو صورة أو صوت تُعامل كمهمة خلفية (job) عبر طابور (queue)، ولا يجوز حجب الواجهة أو الـ API بانتظار اكتمالها. كل مهمة لها حالة قابلة للتتبع (queued → processing → done/failed) وواجهة تستعلم عنها أو تُشعر المستخدم.

### IV. التحكم الصارم بالتكلفة (Cost-Controlled Usage) — NON-NEGOTIABLE
كل استدعاء لمزوّد ذكاء اصطناعي مدفوع مرتبط إلزاميًا بـ: (أ) خصم كريدت من رصيد المستخدم قبل التنفيذ، (ب) سقف استخدام يومي/شهري قابل للضبط، (ج) تسجيل تكلفة فعلية لكل عملية في سجل قابل للتدقيق. لا يُسمح بإطلاق أي ميزة توليد بدون بوابة تحكم بالتكلفة أمامها مباشرة.

### V. تجربة مبنية على اللهجات العربية (Dialect-Aware Experience)
اختيار اللهجة (فصحى، خليجي، عراقي، مصري، شامي) جزء أساسي من كل عملية توليد صوت، وليس إعداد متقدم مخفي. أي مزوّد صوت (TTS) يُضاف للنظام يجب أن يُقيَّم أولًا على جودة اللهجات العربية قبل جودته بالإنجليزية.

### VI. القابلية للاختبار المستقل لكل وحدة (Independent Testability)
كل وحدة وظيفية (إنشاء إعلان صوري، توليد فيديو، إزالة خلفية، تحسين جودة، نظام الكريدت) تُبنى كوحدة يمكن اختبارها وتشغيلها بمعزل عن غيرها، بعقد واجهة (interface contract) واضح. هذا يسمح باستبدال أو تطوير كل جزء دون كسر الباقي، ويسمح بنقل المواصفات (Specs) هذه إلى أي أداة ذكاء اصطناعي أخرى للتنفيذ الجزئي.

---

## القيود التقنية الثابتة (Technical Constraints)

- **الواجهة:** يجب أن تدعم RTL بالكامل كخيار افتراضي، لا كطبقة CSS إضافية.
- **البيانات الحساسة:** بيانات الدفع لا تُخزَّن مباشرة داخل قاعدة بيانات المنصة؛ تُستخدم بوابة دفع خارجية معتمدة (PCI-compliant) دائمًا.
- **الوسائط:** كل ملف وسائط مولَّد (فيديو/صورة/صوت) يُخزَّن في تخزين كائنات (Object Storage) منفصل عن قاعدة البيانات الأساسية.
- **حدود الحجم:** أي عملية توليد فيديو محدودة زمنيًا (مثال: 8–15 ثانية في الباقة المجانية/الأساسية) لضبط التكلفة، وقابلة للتمديد في الباقات الأعلى فقط.

---

## سير العمل والحوكمة (Development Workflow & Governance)

1. لا يبدأ أي تنفيذ (Task) قبل وجود Spec معتمد لنفس الميزة.
2. أي Plan تقني يجب أن يحتوي فحص توافق صريح مع هذا الدستور (Constitution Check) قبل الموافقة عليه.
3. أي انتهاك مبرَّر لمبدأ من المبادئ أعلاه يجب أن يُوثَّق صراحة في قسم "Complexity Tracking" داخل ملف الخطة (plan.md) مع سبب واضح.
4. تعديل هذا الدستور يتطلب زيادة رقم النسخة (Semantic Versioning): MAJOR لتغيير مبدأ جوهري، MINOR لإضافة مبدأ، PATCH لتوضيح صياغة.
